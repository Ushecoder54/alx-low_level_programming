Int add(int a, int b)
{
      
      return (a + b);

}

Int add(int a, int b)
{
      
      return (a - b);

}


Int add(int a, int b)
{
      
      return (a * b);

}


Int add(int a, int b)
{
      
      return (a / b);

}
